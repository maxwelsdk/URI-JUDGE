#include <stdio.h>

int main() {
	char vendedor;
	double salario, vendas;
	double total;
	
	scanf("%s", &vendedor);
	scanf("%lf %lf",&salario, &vendas);
	total = salario+vendas*15/100;
	printf("TOTAL = R$ %.2lf\n", total);	
	
	return 0;
}
