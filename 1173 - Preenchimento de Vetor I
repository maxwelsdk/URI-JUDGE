#include <stdio.h>
 
int main() {
 
	int i, n[10];
	
	scanf("%d", &n[0]);
	printf("N[%d] = %d\n", i, n[i]);
	
	for (i = 1; i < 10; i++){
		n[i] = n[i-1]*2;
		printf("N[%d] = %d\n", i, n[i]);
	} 
    return 0;
}
